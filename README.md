
  # Recreate design in HTML CSS

  This is a code bundle for Recreate design in HTML CSS. The original project is available at https://www.figma.com/design/Vjk2eN8Xhu6L1g9qg7xnK8/Recreate-design-in-HTML-CSS.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  